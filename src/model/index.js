const getMessage = require("./dbMessage");
const getNumber = require("./dbNumber");
const DateTimeBot = require("./clock");

module.exports = {
    getMessage,
    getNumber,
    DateTimeBot,
}
